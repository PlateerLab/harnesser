"""기본 제공 report(분석·설계) 문제 3종 — 풍부한 참고 자료 + 루브릭.

각 문제는 서로 교차 검증되는 자료와 '함정'(SRM·심슨의 역설·검색 실패·동기 전송 등)을
심어, 코드가 아니라 문제해결·분석·설계 능력을 평가하도록 설계됐다.
"""

# ════════════════════════════════════════════════════════════════
#  R2 — 데이터 기반 출시 의사결정 (A/B 테스트)
# ════════════════════════════════════════════════════════════════

AB_STATEMENT = """## 과제 상황

당신은 커머스 팀의 데이터 감각을 갖춘 엔지니어입니다. 새 **체크아웃 플로우(B)**를 기존(A)과 A/B 테스트했고, PM은 이번 주 출시를 원합니다. 제공된 실험 데이터·지표 정의·가드레일을 근거로 **출시 여부를 판단**해야 합니다.

실행 환경이나 원천 로그 전체는 제공되지 않습니다. 대신 **실험 브리프 / 전체·세그먼트 결과 / 트래픽 분배 점검 / 가드레일 지표 / 일별 추세 / 지표 정의 / 통계 노트 / 사전 실험 학습 / PM 메시지**를 참고 자료로 제공합니다.

## 제출물 (필수 4섹션)

Markdown으로 작성하여 제출하세요. 섹션 제목은 그대로 사용하세요.

### 1. 데이터 무결성 점검
- 결과를 신뢰할 수 있는지 먼저 검증 (트래픽 분배·표본·집계 방식 등). 문제가 있으면 그 영향까지 설명.

### 2. 핵심 지표 분석
- 1차 지표를 전체·세그먼트·시간 추세 관점에서 해석. 상충되는 신호가 있으면 원인을 규명.

### 3. 가드레일 점검
- 성능·품질 가드레일이 악화되지 않았는지 근거 기반으로 점검.

### 4. 출시 권고
- **출시 / 조건부 출시 / 보류** 중 하나를 명확히 선택하고, 근거와 (보류라면) 다음 액션을 제시.

## 참고 자료 활용

AI 어시스턴트는 **당신이 지목한 참고 자료만** 열람할 수 있습니다. 예: "srm_check.csv를 열어 트래픽 분배가 기대와 맞는지 확인해줘", "results_overall.csv와 results_by_segment.csv를 대조해서 전체와 세그먼트 결과가 왜 다른지 설명해줘".

## 유의사항

- 통계적 유의성만으로 출시를 정당화하지 마세요. 데이터 무결성·가드레일·의사결정 맥락을 함께 고려해야 합니다.
- 자료에 없는 외부 벤치마크를 사실처럼 인용하지 마세요.
- 평가는 100점(과정 50 + 결과 50)입니다. 모든 인물·수치는 평가용 가상 데이터입니다.
"""

AB_REFS = [
    ("docs/experiment_brief.md", "markdown", """# 실험 브리프 — 체크아웃 플로우 개선 (EXP-2231)

- 가설: 새 체크아웃 플로우(B)가 결제 전환율을 높인다.
- 1차 지표(primary): **결제 전환율** = 체크아웃 진입 대비 결제 완료 비율
- 가드레일(guardrail): p95 응답시간, 결제 오류율, **환불율**(악화 시 출시 불가)
- 대상: 체크아웃 진입 사용자, 50:50 무작위 분배
- 계획: 14일, MDE(최소 검출 효과) +1.5%p, 유의수준 0.05
- 노트: 지난 분기 신규 UI 실험에서 **초반 며칠 지표가 튀었다가 수렴**한 사례가 있었음(prior_experiments 참고).
"""),
    ("data/results_overall.csv", "csv", """variant,users,conversions,conversion_rate
A,60120,12625,21.00%
B,63980,13820,21.60%
"""),
    ("data/results_by_segment.csv", "csv", """variant,segment,users,conversions,conversion_rate
A,mobile,42084,7996,19.00%
B,mobile,38388,7178,18.70%
A,desktop,18036,4629,25.67%
B,desktop,25592,6642,25.95%
-- 주: A는 모바일 비중이 높고, B는 데스크톱 비중이 높다. 데스크톱 전환율이 구조적으로 높음.
"""),
    ("data/srm_check.csv", "csv", """variant,users,expected_share,observed_share,note
A,60120,50%,48.4%,
B,63980,50%,51.6%,SRM 의심 — 카이제곱 p < 0.001
-- SRM(Sample Ratio Mismatch): 무작위 50:50 이어야 하는데 분배가 유의하게 어긋남.
-- 분배가 편향되면 세그먼트 구성이 달라져 전체 결과가 왜곡될 수 있다.
"""),
    ("data/guardrail_metrics.csv", "csv", """variant,p95_latency_ms,error_rate,refund_rate
A,410,0.21%,1.8%
B,690,0.24%,2.6%
-- B는 p95 지연과 환불율이 모두 상승. 환불율은 출시 불가 가드레일.
"""),
    ("data/daily_conversion.csv", "csv", """day,A_conv,B_conv
1,20.9%,23.8%
2,21.1%,23.1%
3,21.0%,22.2%
4,21.0%,21.6%
5,21.1%,21.3%
6,20.9%,21.2%
7,21.0%,21.1%
-- B의 우위가 초반에 컸다가 점차 A 수준으로 수렴(신기 효과 의심).
"""),
    ("docs/metric_definitions.md", "markdown", """# 지표 정의

- 결제 전환율 = (결제 완료 세션) / (체크아웃 진입 세션). 세션 단위, 봇 트래픽은 제외.
- 환불율 = 30일 내 환불 주문 / 결제 완료 주문. 지연 지표라 실험 기간엔 하한 추정치.
- 분배: 사용자(user_id) 해시 기반 배정. 배정은 첫 진입 시 고정.
- 주의: 분배가 user 단위인데 지표는 session 단위로 집계됨 → 사용자당 세션 수가 그룹 간 다르면 편향 가능.
"""),
    ("docs/stats_note.md", "markdown", """# 통계 노트

- 전체 전환율 차이 +0.60%p 는 표본에서 통계적으로 유의(p≈0.01)하나, **SRM이 있으면 유의성 자체가 신뢰할 수 없다**.
- SRM(Sample Ratio Mismatch): 기대 분배와 관측 분배가 유의하게 다른 현상. 배정 로직 버그·리다이렉트 손실·봇 필터 편향 등이 원인. SRM이 있으면 실험 결과를 **폐기하고 원인 수정 후 재실험**하는 것이 표준 관행.
- 심슨의 역설: 전체에서 B가 이겨도 모든 세그먼트에서 B가 질 수 있음(구성 비율 차이 때문). 세그먼트별로 반드시 확인.
- 신기 효과(novelty): 새 UI가 초반에만 반짝 좋아 보이는 현상. 추세 수렴 여부로 판단.
"""),
    ("mail/pm_slack.md", "markdown", """# Slack — #checkout-exp (발췌)

- [월 09:12] PM 지아: 전체 전환율 +0.6%p 유의하게 나왔대요! 이번 주 금요일 100% 출시 목표로 갑시다 🚀
- [월 09:20] 데이터 태호: 잠깐요, 분배가 48.4/51.6 인데 SRM 체크 먼저 해야 할 것 같아요.
- [월 09:24] PM 지아: 그건 사소한 차이 아니에요? 위에서 이번 스프린트 성과로 기대하고 있어서요.
- [월 09:31] 데이터 태호: 환불율도 1.8%→2.6% 올랐어요. 이건 가드레일인데…
"""),
    ("docs/prior_experiments.md", "markdown", """# 사전 실험 학습

- EXP-1980(지난 분기): 새 상품 페이지. 초반 3일 +4% 였다가 7일차에 +0.3%로 수렴 → **신기 효과**로 결론, 출시 보류.
- EXP-2104: 배정 로직 리다이렉트 과정에서 모바일 사용자 일부가 유실되어 SRM 발생 → 결과 폐기, 배정 코드 수정 후 재실험.
- 팀 규칙: 환불율·결제 오류율이 악화되면 전환율이 올라도 출시하지 않는다.
"""),
]

AB_GRADING = {
    "process_weight": 50,
    "result_weight": 50,
    "process": [
        {"name": "무결성 우선 검증", "points": 15, "desc": "지표 해석 전에 SRM·분배·집계 단위 등 데이터 신뢰성을 먼저 점검했는가"},
        {"name": "자료 교차 활용", "points": 15, "desc": "전체·세그먼트·추세·가드레일 자료를 교차 인용해 근거를 구성했는가"},
        {"name": "지시 구체성", "points": 10, "desc": "AI에게 어떤 자료를 어떻게 대조할지 구체적으로 지시했는가"},
        {"name": "분석 깊이", "points": 10, "desc": "심슨의 역설·신기 효과·지연 지표 한계 등 함정을 스스로 짚었는가"},
    ],
    "result": [
        {"name": "SRM 식별", "points": 15, "desc": "SRM으로 결과 신뢰성이 훼손됨을 정확히 지적했는가"},
        {"name": "세그먼트·추세 해석", "points": 12, "desc": "심슨의 역설·신기 효과를 자료로 규명했는가"},
        {"name": "가드레일 판단", "points": 10, "desc": "환불율·지연 악화를 출시 기준에 반영했는가"},
        {"name": "의사결정 명확성", "points": 13, "desc": "보류/조건부 등 결론과 다음 액션이 근거와 함께 명확한가"},
    ],
}


# ════════════════════════════════════════════════════════════════
#  R3 — LLM 챗봇 품질 개선 (RAG 진단)
# ════════════════════════════════════════════════════════════════

LLM_STATEMENT = """## 과제 상황

당신은 AI 개발자입니다. 사내 고객지원 **RAG 챗봇**이 최근 문제가 많다는 피드백을 받았습니다: 잘못된 답변(환각), 정책을 지어냄, 느린 응답, 비용 급증. 실행 환경 대신 **아키텍처 / 시스템 프롬프트 / 대화 트레이스 / 평가 결과 / 검색 설정 / 비용·지연 분해 / 지식베이스 통계 / 사용자 불만 / 과거 시도**를 제공합니다.

이 자료를 근거로 **원인을 진단하고 개선안과 검증 방법을 설계**하세요.

## 제출물 (필수 4섹션)

Markdown으로 작성하여 제출하세요. 섹션 제목은 그대로 사용하세요.

### 1. 문제 진단
- 환각·검색 실패·비용·지연 각각의 원인을 자료 근거로 규명. 어떤 트레이스·지표가 근거인지 명시.

### 2. 개선안
- 프롬프트·청킹·검색(top_k·reranker)·grounding·캐싱 등에서 무엇을 어떻게 바꿀지 근거와 함께 제시. 트레이드오프도 언급.

### 3. 검증 방법 (회귀 방지)
- 개선이 실제로 효과가 있는지, 새 회귀가 없는지 확인할 **평가 설계**(지표·데이터셋)를 제시.

### 4. 우선순위
- 영향/난이도를 고려한 실행 순서.

## 참고 자료 활용

AI 어시스턴트는 **당신이 지목한 참고 자료만** 열람할 수 있습니다. 예: "sample_traces.txt를 열어 환각이 검색 실패 때문인지 프롬프트 때문인지 구분해줘", "retrieval_config.md와 eval_results.csv를 대조해서 top_k와 청킹이 recall에 어떤 영향을 주는지 분석해줘".

## 유의사항

- "모델을 더 큰 걸로 바꾸자"처럼 근거 없는 단순 처방은 낮게 평가됩니다. 자료에 드러난 병목을 짚으세요.
- 평가는 100점(과정 50 + 결과 50)입니다. 모든 데이터는 평가용 가상입니다.
"""

LLM_REFS = [
    ("docs/architecture.md", "markdown", """# RAG 챗봇 아키텍처

```
사용자 질문
  → 임베딩(질문)  [text-embedding-small]
  → 벡터 검색 top_k=3  [pgvector]
  → (reranker 없음)
  → LLM 답변 생성  [gpt-4o-mini, 검색 청크를 프롬프트에 그대로 stuffing]
  → 응답
```

- 지식베이스: 제품 도움말·정책 문서를 청킹해 임베딩 저장.
- reranker·재작성(query rewrite)·인용(citation) 단계는 없음.
"""),
    ("prompts/system_prompt.md", "markdown", """# 현재 시스템 프롬프트

```
당신은 친절한 고객지원 도우미입니다. 아래 참고 문서를 활용해 사용자 질문에 답하세요.
가능한 한 자세하고 확신 있게 답변하세요.

참고 문서:
{retrieved_chunks}

질문: {question}
```

(주: "모르면 모른다고 하라", "문서에 근거하라", "출처를 밝혀라" 같은 지시가 없음.
 오히려 "확신 있게"가 환각을 부추길 수 있음.)
"""),
    ("traces/sample_traces.txt", "text", """=== 트레이스 #1 (환각) ===
Q: 환불은 며칠 안에 신청해야 하나요?
검색된 청크: [배송 정책 문서 일부 — 환불 기한 언급 없음], [FAQ: 배송 조회 방법]
답변: "환불은 구매일로부터 14일 이내에 신청하셔야 합니다."   ← 문서에 없는 수치를 지어냄
(실제 정책: 7일. 관련 청크가 검색되지 않았음)

=== 트레이스 #2 (검색 실패) ===
Q: 기업 회원 세금계산서 발행되나요?
검색된 청크: [개인 회원 가입 안내], [비밀번호 재설정]
답변: "네, 마이페이지 > 세금계산서에서 발행 가능합니다."   ← 근거 청크 없음, 환각
(지식베이스에 기업/세금계산서 문서 자체가 없음 — kb_stats 참고)

=== 트레이스 #3 (검색 성공했으나 무시) ===
Q: 포인트 유효기간은?
검색된 청크: [포인트 정책: 적립 후 12개월 유효], [이벤트 안내]
답변: "포인트는 보통 1~2년 정도 유효합니다."   ← 청크에 12개월이 있는데 반영 안 함
(청크가 너무 길어 핵심이 묻혔을 가능성)

=== 트레이스 #4 (정상) ===
Q: 배송 조회는 어디서 하나요?
검색된 청크: [배송 조회: 마이페이지 > 주문내역]
답변: "마이페이지 > 주문내역에서 조회하실 수 있습니다."   ← 정확
"""),
    ("data/eval_results.csv", "csv", """qid,topic,retrieval_hit,answer_correct,hallucination,latency_ms,input_tokens
1,환불,0,0,1,3200,5200
2,세금계산서,0,0,1,3100,5100
3,포인트,1,0,0,3600,6800
4,배송,1,1,0,1800,3400
5,교환,1,1,0,2000,3600
6,회원등급,0,0,1,3300,5400
7,결제수단,1,1,0,1900,3500
8,쿠폰,1,0,1,3400,6200
-- retrieval_hit=1: 정답 근거 청크가 top_k에 포함됨. 검색 실패(0)에서 환각이 집중.
"""),
    ("docs/retrieval_config.md", "markdown", """# 검색 설정

- 청킹: 고정 2000 토큰, **오버랩 없음**. 문단 경계 무시하고 자름.
- 임베딩: text-embedding-small (질문/문서 동일 모델)
- top_k: 3
- reranker: 없음
- 관측: 청크가 커서 한 청크에 여러 주제가 섞임 → 임베딩이 뭉개짐(트레이스 #3).
  오버랩이 없어 경계에 걸친 정보가 잘림. top_k=3 은 다주제 문서에서 recall이 낮음.
"""),
    ("data/cost_breakdown.csv", "csv", """component,avg_tokens,calls_per_day,cost_share
embedding_query,40,52000,2%
llm_generation_input,5100,52000,71%
llm_generation_output,180,52000,15%
storage_vector,-,-,12%
-- LLM 입력 토큰(청크 stuffing)이 비용의 대부분. 큰 청크 3개를 통째로 넣어 입력이 큼.
"""),
    ("data/latency_trace.csv", "csv", """component,p50_ms,p95_ms
embedding_query,60,120
vector_search,80,180
llm_generation,2400,4100
total,2600,4300
-- 지연의 대부분은 LLM 생성. 입력 토큰이 크면 생성 지연도 증가.
"""),
    ("docs/kb_stats.md", "markdown", """# 지식베이스 통계

- 문서 1,240개, 청크 8,900개.
- 커버리지 점검: '기업 회원/세금계산서', '해외배송' 관련 문서가 **아예 없음**(트레이스 #2 원인).
- 정책 문서는 자주 갱신되나, 갱신 후 재임베딩이 주 1회 배치라 최신성 지연 존재.
"""),
    ("mail/user_complaints.md", "markdown", """# 사용자 불만 분류 (지난 2주)

- "없는 정책을 지어낸다" 34건 (환불 기한·세금계산서 등)
- "답이 느리다" 22건
- "질문과 상관없는 답" 11건
- "맞는데 애매하게 답한다" 9건
"""),
    ("docs/prior_changes.md", "markdown", """# 과거 시도

- temperature 를 0.2→0.8 로 올려봄 → 환각 증가로 롤백.
- 프롬프트에 "친절하게"만 추가 → 효과 없음.
- top_k 를 3→10 으로 임시 상향 시도했으나, 청크가 커서 컨텍스트 초과·비용 급증으로 되돌림.
- 아직 시도 안 함: 청킹 재설계(오버랩), reranker 도입, grounding/refusal 프롬프트, 인용, 캐싱.
"""),
]

LLM_GRADING = {
    "process_weight": 50,
    "result_weight": 50,
    "process": [
        {"name": "RAG 파이프라인 이해", "points": 15, "desc": "임베딩·검색·생성 단계와 각 단계의 실패 양상을 구분해 접근했는가"},
        {"name": "자료 교차 근거", "points": 15, "desc": "트레이스·eval·검색설정·비용·지연 자료를 교차 인용했는가"},
        {"name": "지시 구체성", "points": 10, "desc": "AI에게 환각의 원인(검색 실패 vs 프롬프트)을 구분하도록 구체적으로 지시했는가"},
        {"name": "분석 깊이", "points": 10, "desc": "커버리지 공백·청킹·top_k·최신성 등 다층 원인을 짚었는가"},
    ],
    "result": [
        {"name": "환각 원인 규명", "points": 15, "desc": "검색 실패·근거 무시·프롬프트 부재를 자료로 정확히 구분했는가"},
        {"name": "개선안 실효성", "points": 15, "desc": "청킹/오버랩·top_k·reranker·grounding·refusal·캐싱을 근거·트레이드오프와 함께 제시했는가"},
        {"name": "검증 설계", "points": 12, "desc": "recall@k·faithfulness·회귀셋 등 평가 방법을 설계했는가"},
        {"name": "우선순위", "points": 8, "desc": "영향/난이도를 고려한 실행 순서가 합리적인가"},
    ],
}


# ════════════════════════════════════════════════════════════════
#  R4 — 알림 시스템 설계
# ════════════════════════════════════════════════════════════════

DESIGN_STATEMENT = """## 과제 상황

당신은 백엔드 엔지니어입니다. 빠르게 성장하는 제품의 **알림 시스템(이메일·푸시·SMS)**을 설계해야 합니다. 현재는 요청 처리 경로에서 동기적으로 알림을 보내다 장애가 반복되고 있습니다. 실행 환경 대신 **요구사항 / 현재 상태 / 트래픽 추정 / SLA·SLO / 채널 제약 / 기술 스택 / 장애 이력 / 사용자 선호도** 자료를 제공합니다.

이 자료를 근거로 **확장 가능하고 신뢰할 수 있는 알림 시스템을 설계**하세요.

## 제출물 (필수 5섹션)

Markdown으로 작성하여 제출하세요. 섹션 제목은 그대로 사용하세요.

### 1. 요구사항 정리 & 규모 추정
- 기능/비기능 요구사항을 정리하고, 트래픽 자료로 처리량·피크를 추정.

### 2. 상위 설계
- 컴포넌트와 데이터 흐름(다이어그램 또는 글). 요청 경로에서 발송을 분리하는 방식을 포함.

### 3. 데이터 모델 & API
- 핵심 테이블/스키마와 주요 API(알림 요청·상태 조회·선호도) 개요.

### 4. 신뢰성 설계
- 재시도·**멱등성/중복 방지**·rate limit·장애 격리·순서/지연 처리 등. 장애 이력에서 드러난 문제를 어떻게 막을지.

### 5. 트레이드오프 & 확장
- 선택의 근거와 대안, 향후 확장 지점.

## 참고 자료 활용

AI 어시스턴트는 **당신이 지목한 참고 자료만** 열람할 수 있습니다. 예: "incident_history.md를 열어 과거 장애 패턴을 뽑아줘", "channel_constraints.md와 traffic_estimate.csv를 대조해서 provider rate limit이 피크에 병목이 되는지 계산해줘".

## 유의사항

- 특정 기술 이름 나열이 아니라 **왜 그 선택인지**를 근거로 설명하세요.
- 장애 이력의 문제(중복 발송·provider 장애 전파·thundering herd)를 설계로 반드시 방어하세요.
- 평가는 100점(과정 50 + 결과 50)입니다. 모든 데이터는 평가용 가상입니다.
"""

DESIGN_REFS = [
    ("docs/requirements.md", "markdown", """# 요구사항

## 기능
- 다채널: 이메일·푸시·SMS. 채널별 템플릿.
- 사용자 선호도: 채널 on/off, 조용시간(quiet hours), 빈도 제한(frequency cap), 옵트아웃.
- 트랜잭션 알림(주문 확인 등)과 캠페인 알림(마케팅) 모두.
- 발송 상태 추적(발송/실패/재시도).

## 비기능
- 트랜잭션 알림: 지연에 민감(수 초 내). 캠페인: 처리량 우선.
- 최소 한 번 전달(at-least-once), 단 사용자에게 **중복 발송은 없어야** 함.
- 특정 채널/Provider 장애가 전체를 멈추면 안 됨.
"""),
    ("docs/current_state.md", "markdown", """# 현재 상태 (문제)

- 주문 API 핸들러 안에서 `send_email()`을 **동기 호출**. Provider 응답이 느리면 주문 API가 같이 느려지고, Provider 장애 시 주문까지 실패.
- 재시도 없음. 실패하면 알림이 그냥 유실.
- 캠페인은 for 루프로 순차 발송 → 수십만 건이면 몇 시간, 중간 실패 시 어디까지 보냈는지 모름(중복/누락).
- 멱등성 개념 없음: 배포 롤백/재시도로 같은 알림이 두 번 나간 적 있음.
"""),
    ("data/traffic_estimate.csv", "csv", """type,daily_volume,peak_rps,notes
transactional,1,200,000,450,주문/결제/배송 등. 하루 중 고르게 분포하나 점심·저녁 피크.
campaign,8,000,000,15,000,저녁 일괄 발송 시 순간 폭증(thundering herd).
growth_qoq,+35%,,분기 35% 성장.
"""),
    ("docs/sla_slo.md", "markdown", """# SLA / SLO

- 트랜잭션 알림: 생성 후 p95 5초 내 발송 시도.
- 전달 보장: at-least-once. 사용자 관점 중복은 dedup으로 방지.
- 가용성: 한 채널 Provider 장애가 다른 채널·트랜잭션 경로에 전파되면 안 됨.
- 캠페인: 지연 허용(수십 분), 처리량 우선.
"""),
    ("docs/channel_constraints.md", "markdown", """# 채널/Provider 제약

| 채널 | Provider | Rate limit | 재시도 | 비용 |
|---|---|---|---|---|
| 이메일 | MailX | 500 req/s | 5xx만 재시도 권장 | 저 |
| 푸시 | PushCo | 2,000 req/s | 토큰 만료는 재시도 금지 | 무 |
| SMS | SmsHub | 100 req/s | 중복 전송 요금 부과 | 고 |

- SMS는 rate limit이 낮고(100/s) 중복 발송 시 요금이 이중 청구 → 멱등성 필수.
- 캠페인 피크(15,000 rps)는 어떤 Provider의 rate limit도 초과 → 큐잉·속도 제어 필요.
"""),
    ("docs/tech_stack.md", "markdown", """# 사용 가능한 인프라

- PostgreSQL (주 저장소), Redis (캐시/락/카운터), Kafka (메시지 큐), Kubernetes.
- 새 인프라 도입은 가능하나 근거가 필요. 팀은 Kafka·Redis 운영 경험 있음.
"""),
    ("docs/incident_history.md", "markdown", """# 장애 이력

- INC-77: SMS Provider 지연 → 주문 API 응답시간 급증(동기 호출 때문). 결제 실패 증가.
- INC-83: 배포 롤백 중 캠페인 워커가 재시작되며 이미 보낸 알림을 **다시 발송**(멱등성 부재). 사용자 항의.
- INC-91: 저녁 캠페인 8백만 건을 한꺼번에 큐에 넣어 Provider rate limit 초과·대량 실패(thundering herd). 속도 제어 부재.
- INC-95: 이메일 Provider 일시 장애 시 재시도가 없어 알림 대량 유실.
"""),
    ("docs/user_preferences.md", "markdown", """# 사용자 선호도 규칙

- 채널별 on/off, 옵트아웃(법적 필수 — 마케팅은 옵트아웃 즉시 반영).
- 조용시간(quiet hours): 사용자 타임존 기준 22:00~08:00 캠페인 발송 금지(트랜잭션은 예외).
- 빈도 제한: 마케팅은 사용자당 하루 N건 이하.
- 선호도 조회는 발송 직전 최신값을 반영해야 함(캐시 무효화 고려).
"""),
]

DESIGN_GRADING = {
    "process_weight": 50,
    "result_weight": 50,
    "process": [
        {"name": "요구사항·규모 추정", "points": 15, "desc": "기능/비기능을 정리하고 트래픽 자료로 처리량·피크를 정량 추정했는가"},
        {"name": "자료 교차 활용", "points": 15, "desc": "제약·장애이력·SLA를 교차 인용해 설계 결정을 뒷받침했는가"},
        {"name": "지시 구체성", "points": 10, "desc": "AI에게 병목 계산·장애 패턴 추출 등을 구체적으로 지시했는가"},
        {"name": "설계 깊이", "points": 10, "desc": "트랜잭션/캠페인 분리, 선호도 최신성 등 세부를 고려했는가"},
    ],
    "result": [
        {"name": "아키텍처 적절성", "points": 15, "desc": "요청 경로와 발송을 분리(큐·워커·채널 어댑터)한 확장 가능한 구조인가"},
        {"name": "신뢰성 설계", "points": 15, "desc": "멱등성·중복 방지·재시도·rate limit·장애 격리로 장애 이력을 방어했는가"},
        {"name": "데이터 모델·API", "points": 10, "desc": "핵심 스키마와 API가 요구사항을 충족하는가"},
        {"name": "트레이드오프", "points": 10, "desc": "선택의 근거와 대안·확장 지점을 제시했는가"},
    ],
}


REPORT_PROBLEMS = [
    {
        "title": "데이터 기반 출시 의사결정 (A/B 테스트)",
        "difficulty": "hard",
        "deliverable": "report",
        "statement_md": AB_STATEMENT,
        "reference_files": [{"path": p, "kind": k, "content": c} for (p, k, c) in AB_REFS],
        "grading_criteria": AB_GRADING,
    },
    {
        "title": "LLM 챗봇 품질 개선 (RAG 진단)",
        "difficulty": "hard",
        "deliverable": "report",
        "statement_md": LLM_STATEMENT,
        "reference_files": [{"path": p, "kind": k, "content": c} for (p, k, c) in LLM_REFS],
        "grading_criteria": LLM_GRADING,
    },
    {
        "title": "알림 시스템 설계",
        "difficulty": "hard",
        "deliverable": "report",
        "statement_md": DESIGN_STATEMENT,
        "reference_files": [{"path": p, "kind": k, "content": c} for (p, k, c) in DESIGN_REFS],
        "grading_criteria": DESIGN_GRADING,
    },
]
